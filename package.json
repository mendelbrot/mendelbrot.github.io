{
  "name": "mendelbrot.github.io",
  "version": "2.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "eleventy --serve & npx tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css --watch",
    "build": "eleventy & npx tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.3.1"
  }
}
